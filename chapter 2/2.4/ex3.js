const BIRTHDAY = '18.04.1982' // может писать капсом, потому что это константа, которая не будет изменяться
const age = func(BIRTHDAY) // переменная может измениться, т.к. значение зависит от результата выполнения функции
